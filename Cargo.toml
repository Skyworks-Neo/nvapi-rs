[package]
name = "nvapi"
version = "0.3.0"
publish = false
rust-version = "1.92"
authors = ["arcnmx"]
edition = "2021"

description = "NVIDIA NVAPI bindings"
keywords = ["nvidia", "nvapi", "gtx"]

documentation = "https://docs.rs/nvapi/"
repository = "https://github.com/arcnmx/nvapi-rs"
readme = "README.md"
license = "MIT"

include = [
	"/src/**/*.rs",
	"/tests/**/*.rs",
	"/README*",
	"/COPYING*",
]

[dependencies]
nvapi-sys = { version = "0.3", path = "sys", default-features = false }
serde = { version = "1", features = ["derive"], optional = true }
i2c = { version = "0.1", optional = true }
log = "0.4"
once_cell = "1.12"

[features]
default = ["serde"]
serde = ["dep:serde", "nvapi-sys/serde"]
log = ["nvapi-sys/log"]
log-kv = ["nvapi-sys/log-kv"]

[workspace]
members = ["sys", "macros"]
